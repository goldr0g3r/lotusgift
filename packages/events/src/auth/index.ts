// Per-service event schemas for `services/auth-service` (P5). Empty by
// design at P2 — service phase populates this folder with
// `<event-name>.v1.ts` files per
// `.cursor/rules/event-driven-discipline.mdc`.
export {};
