// Per-service event schemas for `services/customization-service` (P8b).
// `customization.state-changed.v1` mandated by
// `.cursor/rules/corporate-gifting-domain.mdc` lands here at P8b. Empty
// by design at P2.
export {};
