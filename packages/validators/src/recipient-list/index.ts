// Per-service Zod schemas for `services/recipient-list-service` (P9c).
// The fixed-schema recipient row mandated by
// `.cursor/rules/corporate-gifting-domain.mdc` lands here at P9c. Empty
// by design at P2.
export {};
