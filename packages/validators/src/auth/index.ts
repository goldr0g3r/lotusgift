// Per-service Zod schemas for `services/auth-service` (P5). Empty by
// design at P2 — service phase populates this folder with request/response
// schemas alongside the controller implementation per
// `.cursor/rules/api-type-safety.mdc`.
export {};
